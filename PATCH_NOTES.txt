DAILY_INPUT date compatibility fix

Fixes Windows Excel/pywin32 error:
  must be a pywintypes time object (got datetime.date)

The DAILY_INPUT target date is now written to Excel as a midnight datetime,
which COM accepts as an Excel date while preserving the dd/mm/yyyy display.
