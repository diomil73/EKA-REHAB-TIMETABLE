Fixes the workbook day-pattern alias "Καθ/να" so it is interpreted as Monday-Friday.
This restores behavior already required by the read-only Excel adapter and keeps unknown patterns as safety errors.
