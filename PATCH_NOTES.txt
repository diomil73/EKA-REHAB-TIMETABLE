Capacity rebalance planning patch

Adds a read-only planning layer for existing over-capacity base schedules.
It does NOT rewrite the workbook and does NOT automatically choose a patient to move.

Run example:
  python Python/tools/plan_capacity_rebalance.py --date 2026-09-18 --provider "Σκαρώνης"
