DAILY_INPUT Excel locale compatibility fix

- Removes the B2 Range.NumberFormat assignment that fails on some localized Excel/COM installations.
- Writes the target date as deterministic dd/mm/yyyy display text via Value2.
- No change to baseline workbook; this affects preview sheet creation only.
