Provider Policy Patch - Stage 1

Adds provider profiles, therapist/student defaults, temporary replacement exclusion, temporary custom max load, blocked times, Manager/Acting Manager roles, explicit manager replacement times, JSON persistence, CLI, and tests.

This is intentionally additive. Next step is wiring this policy into live replacement ranking, then patient cross-specialty availability.
